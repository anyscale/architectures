name: sample-job-s3-managed
cluster_env: jobs-ce:4
compute_config: S3_Accessing_config
runtime_env:
  working_dir: "s3://dir-temp/arch.zip"
entrypoint: "python ops/sample_job.py --option1=value1"
max_retries: 3
