name: remote_compute_windows_test
cloud: anyscale-managed
runtime_env:
  working_dir: "https://github.com/anyscale/architectures"
entrypoint: "python ops/sample_job.py --option1=value1"
max_retries: 3
